.page-header
  .container
    %h1= @tag.name
    %div
      #{@tag.name}に関する情報が集まっています。
      現在#{@tag.items.count}件の投稿があります。

.container
  - @items.each do |item|
    = render item

  = paginate @items
